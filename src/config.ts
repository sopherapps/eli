/**
 * Module containing the app's configuration
 */

const localStorageAppConfigKey = "eli:AppConfig";
export const tabsKey = `${localStorageAppConfigKey}:tabs`;
export const tabOrderKey = `${localStorageAppConfigKey}:tabOrder`;
