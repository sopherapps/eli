// Component to show the card for editting a tab
import React from "react";
import { Tab } from "../data/models";

export default function TabEditCard(props: { tab: Tab }) {
  return <div>Tab edit card</div>;
}
